# Deploy coming soon page

**Preview first:** open `/coming-soon.html` in the browser (or on your host before swapping).

**Go live in under a minute:** The full homepage is already backed up as `index-full.html`. Run `mv coming-soon.html index.html`, then publish. Restore the full site later with `mv index-full.html index.html`.
